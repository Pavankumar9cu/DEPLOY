{
  "name": "port-urlencounter-formdata",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    
    "build": "yarn --cwd ./client install && yarn --cwd ./client build && yarn install",
    "start": "node --max-old-space-size=4096 app.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "mongoose": "^8.14.1",
    "multer": "^1.4.5-lts.2",
    "nodemon": "^3.1.10"
  }
}
